# 6 - Create DB secrets - still need to fix
apiVersion: v1
kind: Secret
metadata:
  name: database-credentials
  namespace: ibm-common-services
type: Opaque
data:
  db_username: ${{ secrets.BASUSER }}
  db_password: ${{ secrets.ADMIN }}
---
apiVersion: v1
kind: Secret
metadata:
  name: grafana-credentials
  namespace: ibm-common-services
type: Opaque
data:
  grafana_username: ${{ secrets.BASUSER }}
  grafana_password: ${{ secrets.ADMIN }}

